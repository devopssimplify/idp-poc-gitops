---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cockroachdb-pds-505-db
  namespace: dev
  labels:
    app: cockroachdb-pds-505-db
    team: data
    managed-by: backstage
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: cockroachdb-pds-505-db
  namespace: dev
  labels:
    app: cockroachdb-pds-505-db
    team: data
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: cockroachdb-pds-505-db
  namespace: dev
  labels:
    app: cockroachdb-pds-505-db
    team: data
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: cockroachdb-pds-505-db
subjects:
- kind: ServiceAccount
  name: cockroachdb-pds-505-db
  namespace: dev
---
apiVersion: v1
kind: Service
metadata:
  name: cockroachdb-pds-505-db
  namespace: dev
  labels:
    app: cockroachdb-pds-505-db
    team: data
    managed-by: backstage
  annotations:
    description: "CockroachDB public service for pds-505-db"
    prometheus.io/scrape: "true"
    prometheus.io/path: "_status/vars"
    prometheus.io/port: "8080"
spec:
  type: ClusterIP
  ports:
  - port: 26257
    targetPort: 26257
    name: grpc
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: cockroachdb-pds-505-db
---
apiVersion: v1
kind: Service
metadata:
  name: cockroachdb-pds-505-db-headless
  namespace: dev
  labels:
    app: cockroachdb-pds-505-db
    team: data
    managed-by: backstage
  annotations:
    description: "CockroachDB headless service for internal communication"
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
    prometheus.io/scrape: "true"
    prometheus.io/path: "_status/vars"
    prometheus.io/port: "8080"
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
  - port: 26257
    targetPort: 26257
    name: grpc
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: cockroachdb-pds-505-db
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cockroachdb-pds-505-db-budget
  namespace: dev
  labels:
    app: cockroachdb-pds-505-db
    team: data
spec:
  selector:
    matchLabels:
      app: cockroachdb-pds-505-db
  maxUnavailable: 1
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: cockroachdb-pds-505-db
  namespace: dev
  labels:
    app: cockroachdb-pds-505-db
    team: data
    managed-by: backstage
  annotations:
    description: "test"
spec:
  serviceName: cockroachdb-pds-505-db-headless
  replicas: 1
  selector:
    matchLabels:
      app: cockroachdb-pds-505-db
  template:
    metadata:
      labels:
        app: cockroachdb-pds-505-db
        team: data
    spec:
      serviceAccountName: cockroachdb-pds-505-db
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - cockroachdb-pds-505-db
              topologyKey: kubernetes.io/hostname
      containers:
      - name: cockroachdb
        image: cockroachdb/cockroach:v23.1.11
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            cpu: "100m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        ports:
        - containerPort: 26257
          name: grpc
        - containerPort: 8080
          name: http
        livenessProbe:
          httpGet:
            path: "/health"
            port: http
            scheme: HTTP
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: "/health?ready=1"
            port: http
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: datadir
          mountPath: /cockroach/cockroach-data
        env:
        - name: COCKROACH_CHANNEL
          value: kubernetes-insecure
        - name: GOMAXPROCS
          valueFrom:
            resourceFieldRef:
              resource: limits.cpu
              divisor: "1"
        - name: MEMORY_LIMIT_MIB
          valueFrom:
            resourceFieldRef:
              resource: limits.memory
              divisor: "1Mi"
        command:
          - "/bin/bash"
          - "-ecx"
          - |
            # Single node cluster
            exec /cockroach/cockroach start-single-node \
            --logtostderr \
            --insecure \
            --advertise-host $(hostname -f) \
            --http-addr 0.0.0.0 \
            --cache $(expr $MEMORY_LIMIT_MIB / 4)MiB \
            --max-sql-memory $(expr $MEMORY_LIMIT_MIB / 4)MiB
      terminationGracePeriodSeconds: 60
      volumes:
      - name: datadir
        persistentVolumeClaim:
          claimName: datadir
  podManagementPolicy: Parallel
  updateStrategy:
    type: RollingUpdate
  volumeClaimTemplates:
  - metadata:
      name: datadir
      labels:
        app: cockroachdb-pds-505-db
        team: data
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: "5Gi"
      storageClassName: standard-rwo
---
apiVersion: batch/v1
kind: Job
metadata:
  name: cockroachdb-pds-505-db-init
  namespace: dev
  labels:
    app: cockroachdb-pds-505-db
    team: data
    managed-by: backstage
  annotations:
    description: "Initialize CockroachDB cluster"
spec:
  template:
    metadata:
      labels:
        app: cockroachdb-pds-505-db-init
    spec:
      serviceAccountName: cockroachdb-pds-505-db
      restartPolicy: OnFailure
      containers:
      - name: cluster-init
        image: cockroachdb/cockroach:v23.1.11
        imagePullPolicy: IfNotPresent
        command:
          - "/bin/bash"
          - "-ecx"
          - |
            # Wait for DNS to be ready
            until nslookup cockroachdb-pds-505-db-headless.dev.svc.cluster.local; do
              echo "Waiting for DNS..."
              sleep 2
            done
            # Single-node cluster auto-initializes
            echo "Single-node cluster auto-initializes, skipping init..."

            # Wait for cluster to be ready
            sleep 10

            # Create default database
            echo "Creating database pds-505-db..."
            /cockroach/cockroach sql \
            --insecure \
            --host=cockroachdb-pds-505-db-0.cockroachdb-pds-505-db-headless.dev.svc.cluster.local:26257 \
            --execute="CREATE DATABASE IF NOT EXISTS pds-505-db;"

            echo "CockroachDB cluster initialized successfully"
