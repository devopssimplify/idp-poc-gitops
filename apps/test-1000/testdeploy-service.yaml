
apiVersion: v1
kind: Service
metadata:
  name: testdeploy
  namespace: test-1000
  labels:
    app: testdeploy
    team: platform
    version: "1.0"
    managed-by: backstage
  annotations:
    description: "Service for testdeploy"
spec:
  type: ClusterIP
  selector:
    app: testdeploy
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
