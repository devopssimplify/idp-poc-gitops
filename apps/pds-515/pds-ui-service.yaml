
apiVersion: v1
kind: Service
metadata:
  name: pds-ui
  namespace: pds-515
  labels:
    app: pds-ui
    team: backend
    network-code: cn580005
    version: "v1.0"
    managed-by: backstage
  annotations:
    description: "Service for pds-ui"
spec:
  type: ClusterIP
  selector:
    app: pds-ui
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
