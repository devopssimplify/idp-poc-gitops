
apiVersion: v1
kind: Service
metadata:
  name: cnds-ui
  namespace: cnds-502
  labels:
    app: cnds-ui
    team: platform
    network-code: CN580004
    version: "v1.0"
    managed-by: backstage
  annotations:
    description: "Service for cnds-ui"
spec:
  type: ClusterIP
  selector:
    app: cnds-ui
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
